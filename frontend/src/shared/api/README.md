# Shared API client

`apiClient.js` contains the generic fetch wrapper and API base URL.

Feature-specific API functions should live in their feature folder, for example:

- `features/bookings/services/bookingApi.js`
- `features/movies/services/movieApi.js`
- `features/showtimes/services/showtimeApi.js`
