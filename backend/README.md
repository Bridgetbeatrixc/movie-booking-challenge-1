# Beatrix Movie Backend

Express + MongoDB template for the movie booking app.

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

The API runs on `http://localhost:5000` by default.

## Routes

- `GET /api/movies`
- `POST /api/movies`
- `GET /api/bookings`
- `POST /api/bookings`
