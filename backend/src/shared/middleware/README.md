# Shared Middleware

Use this folder for middleware shared by multiple backend modules.

Examples:

- Request validation middleware.
- Error helpers.
- Rate limiting.
- Ownership checks used by more than one module.
