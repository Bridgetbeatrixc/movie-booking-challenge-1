# Shared Utils

Use this folder for utility functions shared by multiple backend modules.

Feature-specific helpers should stay inside their own module. For example, showtime seat validation stays in `src/modules/showtimes/seatValidation.js`.
