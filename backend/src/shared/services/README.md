# Shared services

Cross-cutting backend services live here when they are used by multiple modules or support infrastructure behavior.

- `seedMovies.js` seeds development movie data.
- `ticketService.js` generates tickets, QR codes, PDFs, and email payloads.
- `xenditMock.js` creates the sandbox-style mock QRIS invoice response.
